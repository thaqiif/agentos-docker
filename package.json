{
  "name": "agentos-docker",
  "private": true,
  "scripts": {
    "lint": "cd agent-os && npm run lint",
    "test": "echo 'No tests configured at root level'"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
